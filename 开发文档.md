# 家具软装内部管理平台 — 开发文档

## 1. 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Django + Django REST Framework | Django 5.x, DRF 3.15+ |
| 数据库 | PostgreSQL | 16 |
| 前端框架 | React + TypeScript | React 18, TS 5 |
| UI 组件库 | Ant Design（包豪斯风格定制主题） | 5.x |
| 前端构建 | Vite | 5.x |
| 状态管理 | Zustand | 4.x |
| HTTP 客户端 | Axios | 1.x |
| 路由 | React Router | 6.x |
| Web 服务器 | Nginx（前端）+ Gunicorn（后端） | - |
| 容器化 | Docker Compose | - |
| 测试部署 | Vercel（前端）+ Railway（后端+数据库） | - |
| CI/CD | GitHub → Vercel/Railway 自动部署 | - |
| PDF 生成 | WeasyPrint | 62.x |
| Excel 解析 | openpyxl | 3.x |
| 图片处理 | Pillow | 10.x |

---

## 2. 项目结构

```
├── backend/                    # Django 后端
│   ├── config/                 # Django 配置
│   │   ├── settings.py         # 全局配置（数据库、REST、CORS、缩略图等）
│   │   ├── urls.py             # 根 URL 路由
│   │   └── wsgi.py             # WSGI 入口
│   ├── auth_app/               # 认证与用户管理
│   │   ├── models.py           # User 模型（扩展 AbstractUser，增加 role/display_name）
│   │   ├── views.py            # 登录/登出/用户CRUD API
│   │   ├── serializers.py      # 用户序列化器
│   │   ├── permissions.py      # IsAdminRole 权限类
│   │   ├── services.py         # AuthService（登录/登出/状态切换）
│   │   └── urls.py             # /api/auth/* 路由
│   ├── products/               # 产品管理
│   │   ├── models.py           # Category, Product, ProductImage, ProductConfig, ProductCategory
│   │   ├── views.py            # 产品/分类/配置/图片 CRUD + 批量导入
│   │   ├── serializers.py      # 列表/详情/创建更新序列化器
│   │   ├── services.py         # CategoryService, ProductImageService
│   │   └── urls.py             # /api/products/*, /api/categories/*
│   ├── catalog/                # 产品图册（只读浏览）
│   │   ├── views.py            # CatalogBrowseView, CatalogSearchView
│   │   └── urls.py             # /api/catalog/*
│   ├── cases/                  # 客户案例
│   │   ├── models.py           # Case, CaseImage, CaseProduct
│   │   ├── views.py            # 案例 CRUD + 图片上传
│   │   ├── serializers.py      # 列表/详情/创建更新序列化器
│   │   └── urls.py             # /api/cases/*
│   ├── documents/              # 内部文档
│   │   ├── models.py           # DocumentFolder, Document
│   │   ├── views.py            # 文件夹/文档 CRUD + 上传/下载
│   │   ├── serializers.py      # 文件夹树/文档序列化器
│   │   └── urls.py             # /api/documents/*, /api/document-folders/*
│   ├── quotes/                 # 报价方案
│   │   ├── models.py           # Quote, QuoteItem
│   │   ├── views.py            # 报价单 CRUD + 复制 + PDF 导出
│   │   ├── serializers.py      # 列表/详情/创建更新/明细序列化器
│   │   └── urls.py             # /api/quotes/*, /api/quotes/{id}/items/*
│   ├── sharing/                # 分享功能
│   │   ├── models.py           # ShareLink, ShareAccessLog
│   │   ├── views.py            # 分享链接 CRUD + 公开访问验证
│   │   ├── serializers.py      # 分享链接序列化器
│   │   └── urls.py             # /api/shares/*, /api/share/{token}/*
│   ├── search/                 # 全局搜索
│   │   ├── views.py            # global_search_view（跨模块搜索）
│   │   └── urls.py             # /api/search/
│   ├── common/                 # 公共工具
│   │   ├── file_storage.py     # FileStorageService（文件上传/删除/缩略图）
│   │   ├── pagination.py       # StandardPagination（默认每页20条）
│   │   └── exceptions.py       # 全局异常处理
│   ├── scripts/
│   │   └── create_admin.py     # 初始管理员创建脚本
│   ├── entrypoint.sh           # Docker 启动脚本（迁移+创建admin+启动gunicorn）
│   ├── Dockerfile              # 后端 Docker 构建
│   ├── manage.py               # Django 管理命令
│   └── requirements.txt        # Python 依赖
├── frontend/                   # React 前端
│   ├── src/
│   │   ├── App.tsx             # 路由配置
│   │   ├── main.tsx            # 入口文件
│   │   ├── services/           # API 客户端层
│   │   │   ├── api.ts          # Axios 实例（baseURL=/api, Token拦截器）
│   │   │   ├── authService.ts  # 登录/登出/用户管理 API
│   │   │   ├── productService.ts # 产品/分类/配置/图片 API
│   │   │   ├── caseService.ts  # 案例 API
│   │   │   ├── documentService.ts # 文档/文件夹 API
│   │   │   ├── quoteService.ts # 报价单 API
│   │   │   ├── shareService.ts # 分享链接 API
│   │   │   └── searchService.ts # 全局搜索 API
│   │   ├── store/              # Zustand 状态管理
│   │   │   ├── authStore.ts    # 认证状态（token, user, login, logout）
│   │   │   └── productStore.ts # 产品列表状态（分页、筛选、搜索）
│   │   ├── pages/              # 页面组件
│   │   │   ├── auth/           # LoginPage, UserManagementPage
│   │   │   ├── products/       # ProductListPage, ProductDetailPage, ProductFormPage, CategoryManagementPage
│   │   │   ├── catalog/        # CatalogPage
│   │   │   ├── cases/          # CaseListPage, CaseDetailPage, CaseFormPage
│   │   │   ├── documents/      # DocumentListPage
│   │   │   ├── quotes/         # QuoteListPage, QuoteDetailPage, QuoteFormPage
│   │   │   └── sharing/        # ShareManagementPage, ShareViewPage
│   │   ├── components/         # 公共组件
│   │   │   ├── ProtectedRoute.tsx  # 路由守卫
│   │   │   └── GlobalSearch.tsx    # 全局搜索组件
│   │   └── layouts/            # 布局组件
│   │       ├── MainLayout.tsx  # 主布局（侧边栏+顶栏+内容区）
│   │       └── Sidebar.tsx     # 侧边导航菜单
│   ├── nginx.conf              # Nginx 配置（前端静态文件+API反向代理）
│   ├── Dockerfile              # 前端 Docker 构建（npm build + nginx）
│   ├── package.json            # Node.js 依赖
│   ├── vite.config.ts          # Vite 配置
│   └── tsconfig.json           # TypeScript 配置
├── docker-compose.yml          # Docker 编排（db + backend + frontend）
├── .env.example                # 环境变量模板
├── .gitignore                  # Git 忽略规则
└── docs/                       # 文档
    ├── 部署指南.md              # 完整部署教程
    ├── 需求文档.md              # 需求规格说明
    └── 开发文档.md              # 本文档
```

---

## 3. 数据模型

### 3.1 用户模型 (auth_app.User)

```
User (继承 AbstractUser)
├── username        VARCHAR(150)    用户名（唯一）
├── password        VARCHAR(128)    密码（哈希）
├── role            VARCHAR(10)     角色：ADMIN/STAFF
├── display_name    VARCHAR(100)    显示名称
├── is_active       BOOLEAN         是否启用
└── date_joined     DATETIME        注册时间
```

### 3.2 产品模型

> 本轮变更:产品字段按《产品库字段描述.xlsx》对齐;新增 Brand、ProductConfigDimension、ProductPriceMatrix、ProductPriceRule、ProductDocument 模型,支持电商式配置选购与配置 Excel 动态导入。原有 Category 模型保留作为筛选项字典使用,不再作为强制分类树。

```
Brand（品牌字典）
├── id              BIGINT PK
├── name            VARCHAR(100)    ZIKOO / Steelcase / Vitra / ...
├── is_self_owned   BOOLEAN
├── sort_order      INT
└── created_at      DATETIME

Category（产品分类，沿用作为筛选项字典）
├── id              BIGINT PK
├── name            VARCHAR(100)    分类名称
├── parent          FK → Category   父分类（NULL=顶级）
├── dimension       VARCHAR(10)     维度：TYPE/SPACE/ORIGIN（过渡保留）
├── sort_order      INT             排序序号
└── unique_together: (parent, name, dimension)

Product（产品）
├── id              BIGINT PK
├── name            VARCHAR(200)    产品名称
├── code            VARCHAR(50)     产品编号（唯一，可空）
├── description     TEXT            产品描述
├── category_l1     VARCHAR(20)     一级类别：SEATING/DESKS_WORKSTATIONS/TABLE/STORAGE/ACCESSORIES/EDUCATION
├── category_l2     VARCHAR(40)     二级类别（由一级联动）
├── brand           FK → Brand
├── origin          VARCHAR(10)     产地：IMPORT/DOMESTIC
├── lead_time       VARCHAR(40)     货期：WITHIN_45D / 2-4M_VIETNAM / 2-4M_MALAYSIA / 4-6M_EU
├── pricing_mode    VARCHAR(10)     MATRIX / RULE，默认 MATRIX
├── base_price      DECIMAL(10,2)   仅 RULE 模式使用
├── min_price       DECIMAL(10,2)   最低售价（列表展示）
├── length_mm       INT
├── width_mm        INT
├── height_mm       INT
├── official_url    VARCHAR(500)
├── material_album  JSON            可选材质图册（图片路径数组）
├── model_3d_url    VARCHAR(500)
├── is_active       BOOLEAN         是否上架
├── created_by      FK → User       创建人
├── category        FK → Category   过渡保留（主分类）
├── categories      M2M → Category  过渡保留（多分类）
├── created_at      DATETIME
└── updated_at      DATETIME

ProductImage（产品图片）
├── id              BIGINT PK
├── product         FK → Product
├── image_path      VARCHAR(500)    图片相对路径
├── thumbnail_path  JSON            缩略图路径（small/medium/webp）
├── sort_order      INT             排序
├── is_cover        BOOLEAN         是否封面
└── created_at      DATETIME

ProductConfig（产品配置，旧组合配置兼容保留）
├── id              BIGINT PK
├── product         FK → Product
├── config_name     VARCHAR(200)    配置名称
├── attributes      JSON            自定义属性 {"颜色":"黑色","尺寸":"L"}
├── guide_price     DECIMAL(10,2)   指导价格
└── created_at      DATETIME

ProductConfigDimension（配置维度，动态选择器数据源）
├── id                BIGINT PK
├── product           FK → Product
├── dimension_key     VARCHAR(100)   如 "frame_color_back"
├── dimension_label   VARCHAR(100)   展示名，如 "框架颜色（背框）"
├── options           JSON           [{"key":"P1","label":"P1"}, ...]
├── parent_dimension  VARCHAR(100)   级联父维度 key（可空，如 backrest_series 依赖 backrest_material）
├── is_required       BOOLEAN
├── sort_order        INT
└── created_at        DATETIME
unique_together: (product, dimension_key)

ProductPriceMatrix（模式 A：配置-价格映射表）
├── id                BIGINT PK
├── product           FK → Product
├── config_signature  VARCHAR(255)   配置组合稳定哈希（按 key 排序后 hash）
├── config_attributes JSON           {"frame_color_back":"P1", ...}
├── price             DECIMAL(10,2)
└── created_at        DATETIME
unique_together: (product, config_signature)
index: (product, config_signature)

ProductPriceRule（模式 B：基准价 + 加价规则）
├── id                BIGINT PK
├── product           FK → Product
├── dimension_key     VARCHAR(100)
├── option_key        VARCHAR(100)
├── price_delta       DECIMAL(10,2)  正负皆可
└── sort_order        INT
unique_together: (product, dimension_key, option_key)

ProductDocument（产品-文档关联）
├── id                BIGINT PK
├── product           FK → Product
├── document          FK → Document
├── relation_type     VARCHAR(15)    DESIGN/TRAINING/CERTIFICATE
├── sort_order        INT
└── created_at        DATETIME
unique_together: (product, document)
```

### 3.3 案例模型

```
Case（客户案例）
├── id              BIGINT PK
├── title           VARCHAR(200)    案例标题
├── description     TEXT            项目描述
├── industry        VARCHAR(20)     行业分类（9种）
├── created_by      FK → User
├── products        M2M → Product   关联产品（通过 CaseProduct）
├── created_at      DATETIME
└── updated_at      DATETIME

CaseImage（案例图片）
├── id, case(FK), image_path, thumbnail_path, sort_order, is_cover, created_at
```

### 3.4 文档模型

```
DocumentFolder（文件夹，树形结构）
├── id              BIGINT PK
├── name            VARCHAR(200)
├── doc_type        VARCHAR(15)     DESIGN/TRAINING/CERTIFICATE
├── parent          FK → self
├── sort_order      INT
└── created_at      DATETIME

Document（文档文件）
├── id              BIGINT PK
├── name            VARCHAR(300)    文件名
├── doc_type        VARCHAR(15)     文档类型
├── folder          FK → DocumentFolder
├── file_path       VARCHAR(500)    文件相对路径
├── file_size       BIGINT          文件大小（字节）
├── mime_type       VARCHAR(100)    MIME 类型
├── tags            JSON            标签数组 ["标签1","标签2"]
├── created_by      FK → User
└── created_at      DATETIME
```

### 3.5 报价模型

```
Quote（报价单）
├── id              BIGINT PK
├── title           VARCHAR(200)    报价单标题
├── customer_name   VARCHAR(200)    客户名称
├── status          VARCHAR(15)     DRAFT/SENT/CONFIRMED/CANCELLED
├── notes           TEXT            备注
├── terms           TEXT            条款
├── total_amount    DECIMAL(12,2)   总金额（自动计算）
├── created_by      FK → User
├── created_at      DATETIME
└── updated_at      DATETIME

QuoteItem（报价明细）
├── id              BIGINT PK
├── quote           FK → Quote
├── product         FK → Product    关联产品（可空）
├── product_name    VARCHAR(200)    产品名称（快照）
├── config_name     VARCHAR(200)    配置摘要文本（快照，便于显示）
├── config_attributes JSON          完整配置 {"维度":"选项",...}（电商式选购落库）
├── image           FK → ProductImage  报价明细展示图（来自该产品图集，可空）
├── image_url       VARCHAR(500)    冗余图片路径（导出 PDF 等场景使用）
├── unit_price      DECIMAL(10,2)   单价（按所选配置算出的指导价）
├── quantity        INT             数量（≥1）
├── discount        DECIMAL(5,2)    折扣百分比（0-100）
├── subtotal        DECIMAL(12,2)   小计 = 单价 × 数量 × (1 - 折扣/100)
└── sort_order      INT
```

### 3.6 分享模型

```
ShareLink（分享链接）
├── id              BIGINT PK
├── token           VARCHAR(64)     UUID，唯一索引
├── content_type    VARCHAR(10)     PRODUCT/CASE/QUOTE/CATALOG
├── object_id       INT             关联对象 ID
├── title           VARCHAR(200)    分享标题
├── password_hash   VARCHAR(128)    密码哈希（空=无密码）
├── expires_at      DATETIME        过期时间（NULL=永不过期）
├── max_access_count INT            最大访问次数（NULL=无限制）
├── access_count    INT             已访问次数
├── is_active       BOOLEAN         是否启用
├── created_by      FK → User
└── created_at      DATETIME

ShareAccessLog（访问记录）
├── id, share_link(FK), accessed_at, ip_address, user_agent
```

---

## 4. API 接口

### 4.1 认证 `/api/auth/`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /login/ | 登录，返回 token + user | 公开 |
| POST | /logout/ | 登出，删除 token | 已登录 |
| GET | /me/ | 获取当前用户信息 | 已登录 |
| GET | /users/ | 用户列表 | 管理员 |
| POST | /users/ | 创建用户 | 管理员 |
| GET/PUT | /users/{id}/ | 用户详情/更新 | 管理员 |
| POST | /users/{id}/toggle-status/ | 启用/禁用用户 | 管理员 |

### 4.2 产品 `/api/`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /products/ | 产品列表（支持 search/origin/category_l1/category_l2/brand/lead_time/is_active 筛选） | 已登录 |
| POST | /products/ | 创建产品 | 管理员 |
| GET | /products/{id}/ | 产品详情（含图片、配置维度、关联文档） | 已登录 |
| PATCH | /products/{id}/ | 更新产品 | 管理员 |
| DELETE | /products/{id}/ | 删除产品（软删除） | 管理员 |
| POST | /products/{id}/upload_images/ | 上传产品图片 | 管理员 |
| PUT | /products/{id}/images/order/ | 更新图片排序 | 管理员 |
| POST | /products/import/ | Excel 批量导入 | 管理员 |
| GET | /products/import/template/ | 下载产品导入模板 | 管理员 |
| DELETE | /products/images/{id}/ | 删除图片 | 管理员 |
| PUT | /products/images/{id}/cover/ | 设为封面 | 管理员 |
| GET/POST | /products/{id}/configs/ | 旧配置（兼容保留） | 已登录/管理员 |
| PUT/DELETE | /products/configs/{id}/ | 更新/删除旧配置 | 管理员 |
| **GET** | **/products/{id}/config-dimensions/** | **获取该产品的所有配置维度及选项（动态选择器渲染）** | **已登录** |
| **POST** | **/products/{id}/calculate-price/** | **入参 `{selections:{key:value,...}}`，返回 `{valid, price, breakdown, missing_dimensions, reason}`** | **已登录** |
| **POST** | **/products/{id}/upload-config-excel/** | **上传产品配置 Excel；流程：parse → preview → confirm（query=`?confirm=true`）** | **管理员** |
| **GET** | **/products/config-template/** | **下载产品配置 Excel 标准模板** | **管理员** |
| **GET** | **/products/{id}/documents/** | **获取产品关联的文档（按 relation_type 过滤）** | **已登录** |
| **POST** | **/products/{id}/documents/** | **关联文档 `{document_id, relation_type}`** | **管理员** |
| **DELETE** | **/products/{id}/documents/{doc_id}/** | **解除产品-文档关联** | **管理员** |
| **GET** | **/products/category-options/** | **获取一级 + 二级类别枚举（用于级联下拉与图册筛选）** | **已登录** |

### 4.2.1 品牌 `/api/`（新增）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /brands/ | 品牌列表 | 已登录 |
| POST | /brands/ | 创建品牌 | 管理员 |
| PATCH | /brands/{id}/ | 更新品牌 | 管理员 |
| DELETE | /brands/{id}/ | 删除品牌 | 管理员 |

### 4.3 分类 `/api/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /categories/ | 分类列表（支持 dimension/parent 筛选） |
| POST | /categories/ | 创建分类 |
| GET | /categories/tree/ | 获取分类树（需指定 dimension） |
| PUT | /categories/reorder/ | 批量更新排序 |
| PATCH | /categories/{id}/ | 更新分类 |
| DELETE | /categories/{id}/ | 删除分类 |

### 4.4 图册 `/api/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /catalog/ | 图册浏览（分页，支持多维筛选：brand[]/category_l1[]/category_l2[]/origin[]/lead_time[]/length_range[]/width_range[]/height_range[]/price_range[]/动态属性[]） |
| GET | /catalog/search/ | 图册关键词搜索（名称、编号、材质、尺寸描述） |
| **GET** | **/catalog/filters/** | **筛选项聚合：返回品牌列表 / 一级二级类别 / 产地 / 货期 / MECE 区间预设 / 动态属性聚合** |

### 4.5 案例 `/api/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /cases/ | 案例列表（支持 industry/product_id 筛选） |
| POST | /cases/ | 创建案例 |
| GET | /cases/{id}/ | 案例详情 |
| PATCH | /cases/{id}/ | 更新案例 |
| DELETE | /cases/{id}/ | 删除案例 |
| POST | /cases/{id}/upload_images/ | 上传案例图片 |
| DELETE | /cases/images/{id}/ | 删除案例图片 |

### 4.6 文档 `/api/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /documents/ | 文档列表（支持 doc_type/folder/tag/search 筛选） |
| POST | /documents/upload/ | 上传文档 |
| GET | /documents/{id}/download/ | 下载文档 |
| DELETE | /documents/{id}/ | 删除文档 |
| PATCH | /documents/{id}/ | 更新文档标签 |
| GET | /document-folders/ | 文件夹列表 |
| POST | /document-folders/ | 创建文件夹 |
| GET | /document-folders/tree/ | 文件夹树 |
| DELETE | /document-folders/{id}/ | 删除文件夹 |

### 4.7 报价 `/api/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /quotes/ | 报价单列表（支持 status/search 筛选） |
| POST | /quotes/ | 创建报价单 |
| GET | /quotes/{id}/ | 报价单详情（含明细，明细带 image_url 与 config_attributes） |
| PATCH | /quotes/{id}/ | 更新报价单 |
| DELETE | /quotes/{id}/ | 删除报价单（管理员） |
| POST | /quotes/{id}/duplicate/ | 复制报价单 |
| GET | /quotes/{id}/pdf/ | 导出 PDF（含明细图与配置摘要） |
| GET/POST | /quotes/{id}/items/ | 明细列表/添加（手动录入） |
| **POST** | **/quotes/{id}/items/from-product/** | **一键加入：入参 `{product_id, selections, image_id?, quantity?, discount?}`，后端自动算价 + 写明细 + 重算总价** |
| PUT/DELETE | /quotes/items/{id}/ | 更新/删除明细 |

### 4.8 分享 `/api/`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /shares/ | 分享链接列表 | 已登录 |
| POST | /shares/ | 创建分享链接 | 已登录 |
| DELETE | /shares/{id}/ | 删除分享链接 | 已登录 |
| GET | /share/{token}/ | 获取分享内容（公开） | 公开 |
| POST | /share/{token}/verify/ | 验证分享密码（公开） | 公开 |

### 4.9 搜索 `/api/`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /search/?q=关键词 | 全局搜索（返回 products/cases/documents/quotes） |

---

## 5. 前端路由

| 路径 | 页面 | 权限 |
|------|------|------|
| /login | 登录页 | 公开 |
| /s/:token | 分享查看页 | 公开 |
| / | 首页 | 已登录 |
| /products | 产品列表 | 已登录 |
| /products/new | 新建产品 | 管理员 |
| /products/:id | 产品详情 | 已登录 |
| /products/:id/edit | 编辑产品 | 管理员 |
| /categories | 分类管理 | 已登录 |
| /catalog | 产品图册 | 已登录 |
| /cases | 案例列表 | 已登录 |
| /cases/new | 新建案例 | 管理员 |
| /cases/:id | 案例详情 | 已登录 |
| /cases/:id/edit | 编辑案例 | 管理员 |
| /documents/design | 设计资源 | 已登录 |
| /documents/training | 培训资料 | 已登录 |
| /documents/certificates | 资质文件 | 已登录 |
| /quotes | 报价单列表 | 已登录 |
| /quotes/new | 新建报价单 | 已登录 |
| /quotes/:id | 报价单详情 | 已登录 |
| /quotes/:id/edit | 编辑报价单 | 已登录 |
| /catalog?quoteId=:id | 报价单"新增明细"跳转入口（图册中选品） | 已登录 |
| /users | 用户管理 | 管理员 |
| /shares | 分享管理 | 已登录 |

---

## 6. 认证机制

- 使用 Django REST Framework 的 Token Authentication
- 登录成功后返回 token，前端存储在 localStorage
- 每次请求在 Header 中携带 `Authorization: Token {token}`
- 401 响应自动清除 token 并跳转登录页

---

## 7. 文件存储

- 上传文件存储在 `MEDIA_ROOT` 目录下
- 图片上传后自动生成缩略图（small: 150×150, medium: 400×400）
- 文件路径格式：`{subdirectory}/{YYYYMMDD}/{uuid}_{filename}`
- 仅支持本地磁盘存储（不使用云对象存储）
- 测试环境（Railway）：使用 Railway 持久化存储卷
- 生产环境（NAS）：Docker volume 挂载到 NAS 本地磁盘

---

## 8. 部署架构

### 8.1 测试环境：Vercel + Railway（GitHub 自动部署）

```
GitHub Repository
  ├── 推送代码 → 自动触发部署
  │
  ├── Vercel（前端）
  │   ├── 自动构建 React 项目（npm run build）
  │   ├── 静态文件托管 + CDN 加速
  │   ├── 环境变量: VITE_API_BASE_URL=https://<railway-backend>.railway.app
  │   └── 自定义域名（可选）
  │
  └── Railway（后端 + 数据库）
      ├── Django + Gunicorn
      │   ├── 自动检测 Python 项目并部署
      │   ├── 启动命令: gunicorn config.wsgi --bind 0.0.0.0:$PORT
      │   ├── 静态文件: WhiteNoise 中间件直接服务
      │   ├── 媒体文件: Railway 持久化存储卷
      │   └── 环境变量: DATABASE_URL, DJANGO_SECRET_KEY, CORS 等
      └── PostgreSQL 插件
          ├── Railway 内置 PostgreSQL 服务
          └── 自动注入 DATABASE_URL 环境变量
```

**测试环境特点**：
- 推送到 GitHub main 分支自动部署，无需手动操作
- 前后端分离部署，前端通过环境变量配置 API 地址
- Railway 提供免费的 PostgreSQL 数据库
- 适合开发测试和演示

### 8.2 生产环境：Synology NAS Docker Compose

```
docker-compose.yml 定义 3 个服务：

db (postgres:16-alpine)
  ├── 端口: 5432
  ├── 数据: postgres_data volume
  └── 环境变量: POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD

backend (Django + Gunicorn)
  ├── 端口: 8000
  ├── 启动: entrypoint.sh → makemigrations → migrate → create_admin → gunicorn
  ├── 数据: media_data, static_data volumes
  └── 依赖: db

frontend (React + Nginx)
  ├── 端口: 80
  ├── Nginx 配置:
  │   ├── / → 前端静态文件
  │   ├── /api/* → 反向代理到 backend:8000
  │   ├── /media/* → 媒体文件目录
  │   └── /static/* → Django 静态文件
  └── 依赖: backend
```

**生产环境特点**：
- 部署在客户 Synology DS920+ NAS 上
- 局域网直接访问 + 外网通过 QuickConnect/端口映射/DDNS 访问
- Docker Compose 一键部署，容器设置 restart: always 开机自启
- 媒体文件通过 NAS Hyper Backup 备份

---

## 9. 关键业务逻辑

### 9.1 报价单金额计算
- 每个明细项：`subtotal = unit_price × quantity × (1 - discount / 100)`
- 总金额：所有明细项 subtotal 之和
- 在 QuoteItem.save() 中自动计算 subtotal
- 在增删改明细后调用 Quote.recalculate_total() 更新总金额

### 9.2 分享链接验证流程
1. 前端访问 `/s/{token}`
2. 调用 `GET /api/share/{token}/` 获取分享信息
3. 如果需要密码，显示密码输入框
4. 调用 `POST /api/share/{token}/verify/` 验证密码
5. 验证通过后显示分享内容，同时记录访问日志

### 9.3 产品搜索逻辑
搜索关键词同时匹配以下字段（OR 关系）：
- 产品名称 (name)
- 产品编号 (code)
- 产品描述 (description)
- 配置维度选项值（聚合自 ProductConfigDimension.options）
- 旧 ProductConfig（兼容保留）

### 9.4 类别与品牌筛选
- 一级 / 二级类别枚举固定，由后端常量维护，前端通过 `/api/products/category-options/` 获取
- 二级类别由一级联动（前端级联下拉）
- 品牌通过 `/api/brands/` 维护，作为筛选项与产品必填字段

### 9.5 产品配置实时计算指导价（核心链路 — 第 1 步）

**入口**：`POST /api/products/{id}/calculate-price/`，入参 `{selections: {dimension_key: option_key, ...}}`

```python
def calculate_price(product, selections: dict) -> dict:
    # 1. 校验必填维度齐全
    required_dims = ProductConfigDimension.objects.filter(
        product=product, is_required=True
    )
    missing = [d.dimension_key for d in required_dims if d.dimension_key not in selections]
    if missing:
        return {'valid': False, 'price': None, 'missing_dimensions': missing}

    # 2. 校验级联约束（子维度只能在父维度选中后选择，且选项属于父选项的子集）
    for dim in ProductConfigDimension.objects.filter(product=product):
        if dim.parent_dimension and dim.parent_dimension in selections:
            # 校验当前选项在该父选项允许的子集内（options 内含 parent_value 关联）
            ...

    # 3. 模式 A：查映射表
    if product.pricing_mode == 'MATRIX':
        sig = build_signature(selections)  # 按 key 排序后哈希
        row = ProductPriceMatrix.objects.filter(
            product=product, config_signature=sig
        ).first()
        if not row:
            return {'valid': False, 'price': None, 'reason': '该组合无对应价格'}
        return {'valid': True, 'price': row.price, 'breakdown': selections}

    # 4. 模式 B：基准价 + 加价
    price = product.base_price
    deltas = []
    for k, v in selections.items():
        rule = ProductPriceRule.objects.filter(
            product=product, dimension_key=k, option_key=v
        ).first()
        if rule:
            price += rule.price_delta
            deltas.append({'dimension': k, 'option': v, 'delta': rule.price_delta})
    return {
        'valid': True,
        'price': price,
        'breakdown': {'base_price': product.base_price, 'deltas': deltas},
    }
```

**约束**：
- 接口响应时间需 < 300ms
- 模式 A 下无映射的组合 → UI 提示并禁用「加入报价单」按钮
- 模式 B 下当前选项无对应规则 → 视为 delta=0

### 9.6 一键加入指定报价单（核心链路 — 第 2 步）

**入口**：`POST /api/quotes/{quote_id}/items/from-product/`

```python
@transaction.atomic
def add_item_from_product(quote, product, selections, image_id, quantity=1, discount=0):
    # 1. 复用价格计算
    result = calculate_price(product, selections)
    if not result['valid']:
        raise ValidationError(result)

    # 2. 选定明细图（必须属于该产品图集）
    image = None
    image_url = ''
    if image_id:
        image = ProductImage.objects.get(pk=image_id, product=product)
        image_url = image.image_path

    # 3. 写入明细
    item = QuoteItem.objects.create(
        quote=quote,
        product=product,
        product_name=product.name,
        config_name=summarize_selections(product, selections),  # 配置摘要文本
        config_attributes=selections,
        unit_price=result['price'],
        quantity=quantity,
        discount=discount,
        image=image,
        image_url=image_url,
        sort_order=quote.items.count(),
    )
    # subtotal 在 QuoteItem.save() 中自动计算

    # 4. 触发总价重算
    quote.recalculate_total()
    return item
```

### 9.7 产品配置 Excel 导入（OPT-6.x）

平台提供标准模板（`GET /api/products/config-template/`），含三种 sheet：
- `dimensions`：维度定义（dimension_key / dimension_label / options / parent_dimension / is_required / sort_order）
- `pricing_mode`：mode（MATRIX / RULE）+ base_price（仅 RULE）
- `matrix`（模式 A）或 `rules`（模式 B）：组合-价格 / 加价规则

导入流程：`POST /api/products/{id}/upload-config-excel/`
1. 上传 → 后端解析 → 返回预览（成功条数 / 失败条数 / 错误清单）
2. 前端确认 → 重新调用接口（`?confirm=true`）→ 写入 ProductConfigDimension + ProductPriceMatrix/Rule
3. 同一产品再次导入采用「先清空后写入」策略

---

## 10. 环境变量

### 10.1 通用环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| DJANGO_SECRET_KEY | Django 密钥 | dev-secret-key |
| DJANGO_DEBUG | 调试模式 | True |
| DJANGO_ALLOWED_HOSTS | 允许的主机名 | localhost,127.0.0.1 |
| CORS_ALLOWED_ORIGINS | CORS 允许的源 | http://localhost |
| ADMIN_USERNAME | 初始管理员用户名 | admin |
| ADMIN_PASSWORD | 初始管理员密码 | admin123456 |

### 10.2 数据库环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| DATABASE_URL | 数据库连接URL（Railway 优先使用） | - |
| DB_NAME | 数据库名（Docker 部署使用） | furniture_app |
| DB_USER | 数据库用户 | postgres |
| DB_PASSWORD | 数据库密码 | postgres |
| DB_HOST | 数据库主机 | localhost |
| DB_PORT | 数据库端口 | 5432 |

### 10.3 存储环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| MEDIA_ROOT | 媒体文件目录 | /app/media |

### 10.4 前端环境变量（Vercel）

| 变量 | 说明 | 默认值 |
|------|------|--------|
| VITE_API_BASE_URL | 后端 API 地址 | http://localhost:8000 |

**说明**：
- Railway 部署时优先使用 `DATABASE_URL`（Railway 自动注入），如不存在则使用 `DB_*` 系列变量
- 仅支持 PostgreSQL 数据库
- 媒体文件统一使用本地磁盘存储（测试环境使用 Railway 持久化卷，生产环境使用 NAS 本地磁盘）


---

## 11. 电商式选购 — 前端组件与 Excel 模板

> 对应需求文档 §3 核心业务流程，落地到前端组件与配置 Excel 标准格式。

### 11.1 前端组件清单（新增 / 改造）

| 组件 | 位置 | 说明 |
|---|---|---|
| `<ProductConfigSelector />` | products/ProductDetailPage | 动态渲染配置维度选择器，支持级联，debounce 触发 calculate-price |
| `<ProductPriceDisplay />` | products/ProductDetailPage | 实时显示价格 + 配置明细面包屑；无效组合时显示提示 |
| `<AddToQuoteModal />` | products/ProductDetailPage | 弹窗：选择目标报价单（已有列表/新建）+ 数量/折扣 + 触发图集选图 |
| `<ProductImagePicker />` | 公共组件 | 从产品图集网格中选定一张图（用作明细展示图） |
| `<QuoteItemEditor />` | quotes/QuoteDetailPage | 明细 CRUD UI（行内编辑或弹窗编辑数量/折扣/图） |
| `<QuoteItemAddEntry />` | quotes/QuoteDetailPage | 「新增明细」按钮，跳转 `/catalog?quoteId={id}` |
| `<CatalogFilterDrawer />` | catalog/CatalogPage | 悬浮筛选面板（淘宝式），含 MECE 区间滑块、动态属性聚合 |
| `<CatalogCategoryTabs />` | catalog/CatalogPage | 多级类别标签展示（一级 → 二级），与筛选状态联动 |
| `<ConfigExcelImporter />` | products/ProductFormPage | 上传产品配置 Excel + preview + confirm |
| `<ProductDocumentList />` | products/ProductDetailPage | 关联文档分组展示（设计资源/培训资料/资质文件）+ 在线预览 |
| `<RichTextUploader />` | documents/* | 培训资料富文本编辑器（图文混排 + 视频/音频内嵌） |
| `<MediaPreview />` | documents/* + products/* | 在线预览：图片 / PDF / 视频 / 音频 / Office |

### 11.2 productService 与 quoteService 新增 API（前端 axios）

```ts
// productService（新增）
getConfigDimensions: (productId) => api.get(`/api/products/${productId}/config-dimensions/`),
calculatePrice: (productId, selections) =>
    api.post(`/api/products/${productId}/calculate-price/`, { selections }),
uploadConfigExcel: (productId, file, confirm = false) => {
    const fd = new FormData(); fd.append('file', file);
    return api.post(
        `/api/products/${productId}/upload-config-excel/${confirm ? '?confirm=true' : ''}`,
        fd,
        { headers: { 'Content-Type': 'multipart/form-data' } }
    );
},
downloadConfigTemplate: () => api.get('/api/products/config-template/', { responseType: 'blob' }),
getProductDocuments: (productId, params) =>
    api.get(`/api/products/${productId}/documents/`, { params }),
linkDocument: (productId, payload) => api.post(`/api/products/${productId}/documents/`, payload),
unlinkDocument: (productId, docId) =>
    api.delete(`/api/products/${productId}/documents/${docId}/`),
getCategoryOptions: () => api.get('/api/products/category-options/'),

// quoteService（新增）
addItemFromProduct: (quoteId, payload) =>
    api.post(`/api/quotes/${quoteId}/items/from-product/`, payload),
// payload = { product_id, selections, image_id?, quantity?, discount? }

// brandService（新增）
getBrands: () => api.get('/api/brands/'),
createBrand: (data) => api.post('/api/brands/', data),
updateBrand: (id, data) => api.patch(`/api/brands/${id}/`, data),
deleteBrand: (id) => api.delete(`/api/brands/${id}/`),

// catalog（新增）
getCatalogFilters: () => api.get('/api/catalog/filters/'),
```

### 11.3 产品配置 Excel 模板（OPT-6.1 草案）

**Sheet 1 — `dimensions`（维度定义）**

| dimension_key | dimension_label | options（逗号分隔） | parent_dimension | is_required | sort_order |
|---|---|---|---|---|---|
| frame_color_back | 框架颜色（背框） | P1,P2,P3 |  | TRUE | 1 |
| backrest_material | 靠背材质 | 网,布,皮 |  | TRUE | 2 |
| backrest_series | 靠背材质系列 | AIR,3D knit,intermix | backrest_material=网 | FALSE | 3 |
| armrest | 扶手 | 2D,3D,4D |  | TRUE | 4 |

> `parent_dimension` 列允许填写 `父维度=父选项` 表达级联约束；当父维度选某值时该子维度才出现，且选项限定为该子集。

**Sheet 2 — `pricing_mode`**

| mode | base_price |
|---|---|
| MATRIX | （留空） |
| 或 RULE | 1980 |

**Sheet 3a — `matrix`（模式 A 用）**

| frame_color_back | backrest_material | backrest_series | armrest | price |
|---|---|---|---|---|
| P1 | 网 | AIR | 3D | 2580 |
| P1 | 网 | AIR | 4D | 2780 |
| P2 | 皮 |  | 4D | 3580 |
| ... | ... | ... | ... | ... |

**Sheet 3b — `rules`（模式 B 用）**

| dimension_key | option_key | price_delta |
|---|---|---|
| frame_color_back | P1 | 0 |
| frame_color_back | P2 | 50 |
| backrest_material | 皮 | 800 |
| armrest | 4D | 200 |

### 11.4 验收标准（核心链路端到端）

- ✅ 管理员上传配置 Excel 后，产品详情页能动态渲染配置选择器
- ✅ 用户每次更改配置，价格在 300ms 内更新
- ✅ 模式 A 下选到无对应映射的组合时，UI 友好提示"此组合无报价"且禁用「加入报价单」按钮
- ✅ 模式 B 下基准价 + 各维度 delta 累加正确
- ✅ 「加入报价单」可选择已存在报价单或快速新建
- ✅ 加入时可从产品图集中选定一张图作为明细展示图
- ✅ 报价单明细落库后，Quote.total_amount 自动重算
- ✅ 报价单详情页可对该明细 编辑数量/折扣/图片/删除
- ✅ 报价单详情页「新增明细」按钮跳转 `/catalog?quoteId={id}`，在图册中选品并完成上述流程
- ✅ 报价单 PDF 导出时，明细行包含展示图与配置摘要

### 11.5 迁移与兼容策略

- 旧 `ProductConfig`（一行=完整配置）保留，作为没有上传配置 Excel 的产品的默认指导价数据源
- 详情页加载逻辑：
  1. 优先读取 `ProductConfigDimension`（电商式选购）
  2. 若无维度数据，回退到旧 `ProductConfig` 列表（仅展示，不支持电商式选购）
- 旧 `Category` 三维度（TYPE/SPACE/ORIGIN）数据保留过渡，新功能使用 `category_l1 / category_l2 / brand` 字段
