# 家具软装内部管理平台 — 需求文档

## 1. 项目概述

### 1.1 项目背景
家具软装公司内部使用的资料管理系统,用于管理产品信息、客户案例、内部文档、报价方案,并支持通过分享链接将产品/报价信息发送给外部客户查看。

### 1.2 目标用户
- **管理员**:公司管理层,拥有全部权限(增删改查、用户管理)
- **普通员工**:销售/设计人员,拥有查看权限和部分创建权限
- **外部客户**:通过分享链接访问,无需注册登录

### 1.3 部署环境

#### 测试环境
- **前端**: Vercel(自动从 GitHub 部署,CDN 加速)
- **后端**: Railway(Django + Gunicorn + PostgreSQL)
- **CI/CD**: 推送到 GitHub main 分支后自动部署到 Vercel 和 Railway
- **用途**: 开发测试和功能验证

#### 生产环境
- **目标硬件**: Synology DS920+ NAS
- **部署方式**: Docker Compose 一键部署
- **访问方式**: 局域网直接访问 + 外网通过 QuickConnect/端口映射/DDNS 访问

---

## 2. 功能模块

### 2.1 用户认证与权限

| 功能 | 说明 |
|------|------|
| 登录/登出 | 用户名+密码登录,Token 认证 |
| 角色体系 | 管理员(ADMIN)、普通员工(STAFF)两种角色 |
| 权限控制 | 管理员可增删改查所有数据;员工只能查看,不能删除 |
| 用户管理 | 管理员可创建/编辑/启用/禁用用户账号 |
| 初始账号 | 系统启动时自动创建管理员账号(来自环境变量) |

### 2.2 产品管理

> 字段结构按《产品库字段描述.xlsx》对齐(详见 §2.2.2 字段清单与动态表单)。

#### 2.2.1 基础功能

| 功能 | 说明 |
|------|------|
| 产品 CRUD | 创建/编辑/删除/查看产品 |
| 一级 / 二级类别(级联) | 6 大一级类别(Seating / Desks+Workstations / Table / Storage / Accessories / Education),选定一级后二级动态联动 |
| 品牌字典 | ZIKOO 自有品牌 + 国际品牌(Steelcase / Vitra 等),可由管理员维护 |
| 产地分类 | 进口、国产 |
| 货期 | 45 天内 / 2-4 月【越南】 / 2-4 月【马来西亚】 / 4-6 月【荷兰/意大利/德国】 |
| 品类驱动动态表单 | 二级类别决定后续展开的专属字段集(如椅子类展开框架颜色 / 坐垫材质 / 靠背材质等) |
| 产品图片 | 多图上传、封面设置、排序、缩略图自动生成、三视图组织 |
| 多配置价格 | 见 §2.2.3 配置维度与配置-价格双模式 |
| 产品配置 Excel 导入 | 见 §2.2.4 |
| 产品-文档关联 | 关联设计资源 / 培训资料 / 资质文件,详情页直接展示 |
| 全字段搜索 | 名称、编号、描述、配置维度值等 |
| 上架/下架 | 管理员可控制产品是否对员工可见 |
| 批量导入 | 通过 Excel 文件批量导入产品(模板下载+上传解析) |

#### 2.2.2 字段清单(对齐 xlsx)

**通用必填**:产品名称、产品编号、产品分类(一级 / 二级)、品牌、产地、货期、产品描述、指导价、产品图片(三视图,选一张作首图)。

**通用可选**:长度、宽度、高度、官网链接、可选材质图册、3D 模型链接。

**椅子类专属字段(动态表单示例)**:框架颜色(背框)、框架颜色(座框)、坐垫材质、靠背材质(级联系列:网/布/皮 → AIR/3D knit/intermix 等)、扶手(2D/3D/4D)、底座(标准/高配/低配)、腰撑、滚轮(地板轮/地毯轮)、头枕、脚踏。

#### 2.2.3 配置维度与配置-价格双模式

每个产品的可选配置由若干 **配置维度** 组成(每个维度有可选项,部分维度之间存在级联约束)。系统支持两种指导价计算模式,产品级二选一:

- **模式 A:配置-价格映射表**
  - 每个产品维护"配置组合 → 价格"映射表
  - 价格直接查表,不存在的组合视为无效配置(UI 提示并禁用加入报价单)
  - 适合规格组合有限、价格不规律的产品(如 xlsx 中的 A 方案)
- **模式 B:基准价 + 加价规则**
  - 系统记录基准价
  - 每个配置维度的每个选项有独立加价值(可正可负)
  - 用户更改任一选项 → 实时按"基准价 + 累加加价值"重算
  - 适合规格组合多、价格可累加的产品(对应 xlsx 中的 B 方案)

#### 2.2.4 产品配置 Excel 动态导入

- 平台提供标准 Excel 模板(包含维度定义 / 模式声明 / 映射或加价规则三种 sheet)
- 管理员在产品详情页/编辑页上传该产品的配置 Excel
- 后端解析 → 预览(校验报告) → 确认导入,数据写入配置维度表 + 映射表(模式 A)或加价规则表(模式 B)
- 导入完成后,产品详情页的配置选择器 **动态渲染** 实际可选维度

### 2.3 产品图册(对外/对内浏览入口)

> 不再使用左侧分类树导航,改为多维搜索 + 悬浮筛选(淘宝首页式),即时查询。

| 功能 | 说明 |
|------|------|
| 卡片浏览 | 以卡片形式展示所有上架产品,封面图、名称、编号、品牌、起始价 |
| 关键词搜索 | 顶部搜索框检索产品名称、编号、材质描述、尺寸描述等不在筛选项里的属性细节 |
| 多维悬浮筛选 | 浮层筛选面板:品牌、一级类别、二级类别、产地、货期、长 / 宽 / 高 等;支持多选 |
| 多级类别快捷展示 | 顶部多级类别标签,选中一级展开二级,已选条件以面包屑可移除 |
| 即时查询 | 输入或勾选立即触发查询(debounce),前端实时刷新结果,无需点确认 |
| MECE 区间筛选 | 连续型变量(尺寸、价格)预配置互斥且穷尽的区间(如长度 ≤600 / 600-900 / 900-1200 / 1200-1800 / >1800 mm) |
| 动态筛选项 | 除人工配置字段(品牌/产地/货期等)外,其它属性筛选项基于产品库实际数据动态聚合生成 |
| 分页 | 分页加载,避免大量数据卡顿 |

### 2.4 产品详情页(电商式选购体验)

> 完整重构,作为"选品 → 算价 → 加单"的核心入口。

| 区块 | 说明 |
|------|------|
| 产品头图区 | 大图展示 + 缩略图列表(三视图) |
| 基础信息 | 编号、品牌、产地、货期、描述、官网链接、3D 模型链接、可选材质图册 |
| 配置选择器 | 按上传的配置维度动态渲染选项,支持级联;每次更改实时调用价格计算 API |
| 实时指导价 | 显示当前组合的最终指导价 + 配置明细面包屑;无效组合时提示并禁用加入报价单 |
| 加入报价单 | 一键加入指定报价单:弹窗选目标报价单(已有列表 + 新建入口)→ 弹窗从该产品图集中选定一张明细展示图 → 提交后写入明细并自动重算总价 |
| 关联文档 | 设计资源 / 培训资料 / 资质文件分类展示,支持在线预览(图片/PDF/视频/音频/Office) |

### 2.5 客户案例

| 功能 | 说明 |
|------|------|
| 案例 CRUD | 创建/编辑/删除/查看客户案例 |
| 行业树筛选 | 办公空间 8 大细分行业 + 其他,以树形/标签筛选 |
| 排序 | 默认按案例名称升序 |
| 案例图片 | 多图上传、封面设置、缩略图;**详情页图片平铺加载,不分页**,启用懒加载 / 缩略图 / WebP 优化 |
| 关联产品 | 案例可关联多个产品,详情页展示关联产品信息 |

### 2.6 内部文档(设计资源 / 培训资料 / 资质文件)

| 功能 | 说明 |
|------|------|
| 文件夹管理 | 树形文件夹结构,支持创建 / 删除文件夹(**不再做拖拽排序**) |
| 文件上传 | 支持上传任意类型文件,最大 50MB |
| 文件下载 | 直接下载 |
| 在线预览 | 设计资源、培训资料、资质文件均支持在线预览:图片(模态框)、PDF(浏览器内嵌)、视频(HTML5)、音频(HTML5)、Office 文档(预览服务) |
| 培训资料富文本 | 培训资料支持富文本上传(图文混排),并支持视频、音频等格式作为内嵌或独立资源 |
| 文件搜索 | 按文件名搜索 |
| 标签系统 | 文件可添加标签,按标签筛选 |

### 2.7 报价方案

| 功能 | 说明 |
|------|------|
| 报价单 CRUD | 创建/编辑/删除报价单,包含标题、客户名称、备注、条款 |
| 明细 CRUD | 增、删、改、查报价明细;支持调整数量、折扣、单价、明细展示图 |
| 一键加入明细 | 从产品详情页的"加入报价单"动作直接落入明细,自动按所选配置计算单价 |
| 明细展示图 | 每条明细可绑定一张来自产品图集的图片,在报价单内/PDF 中显示 |
| 跳转新增 | 报价单详情页「新增明细」按钮跳转产品图册(带 quoteId 上下文),在图册中选品后回流 |
| 价格计算 | 单价 × 数量 × (1 - 折扣/100) 自动计算小计,总金额自动汇总 |
| 状态管理 | 草稿 → 已发送 → 已确认 → 已取消 |
| 复制报价单 | 一键复制现有报价单(含所有明细) |
| PDF 导出 | 生成 PDF 格式的报价单,包含每条明细的展示图与配置摘要 |
| 搜索筛选 | 按标题/客户名搜索,按状态筛选 |

### 2.8 分享功能

| 功能 | 说明 |
|------|------|
| 创建分享链接 | 可分享产品、案例、报价单、产品图册 |
| 访问控制 | 可设置密码保护、过期时间、最大访问次数 |
| 启用/禁用 | 可随时启用或禁用分享链接 |
| 公开访问 | 外部客户通过链接直接访问,无需登录 |
| 访问记录 | 记录每次访问的时间、IP、浏览器信息 |
| 分享链接格式 | `http://域名/s/{uuid}` |
| 批量分享 | 支持分享一组对象(如某行业案例集合) |
| 品牌展示 | 分享页面展示公司 Logo / 名称 / 联系方式 |

### 2.9 全局搜索

| 功能 | 说明 |
|------|------|
| 跨模块搜索 | 同时搜索产品、案例、文档、报价单 |
| 实时搜索 | 顶部搜索栏输入关键词,下拉展示结果 |
| 结果跳转 | 点击搜索结果直接跳转到对应详情页 |

---

## 3. 核心业务流程:产品配置 → 实时算价 → 加入报价单

```
管理员侧                                 销售/设计侧
─────────                                ─────────
1. 录入产品(按 xlsx 字段)
2. 上传产品配置 Excel(模板)
   → 解析 → 预览 → 确认导入
3. 上传产品图集(三视图等)
4. 关联文档资源

                    ─────→ 5. 销售打开产品详情页
                              查看大图、基础信息、配置选择器、关联文档

                            6. 在配置选择器逐个维度选择
                              (材质 / 颜色 / 扶手 / 底座 ...)
                              → 系统实时计算并显示指导价 + 配置明细

                            7. 点击「加入报价单」
                              → 选择目标报价单(已有列表/新建)
                              → 从该产品图集中选定一张展示图
                              → 提交

                            8. 系统自动:
                              · 落明细(产品 + 配置 + 单价 + 图片 + 数量)
                              · 重算报价单总额

                            9. 报价单详情页可继续 CRUD 明细
                              · 调整数量 / 折扣 / 明细图
                              · 「新增明细」跳回产品图册(带 quoteId)
```

**关键约束**:
- 模式 A 下若选到映射表中不存在的组合,UI 友好提示并禁用「加入报价单」
- 模式 B 下基准价 + 各维度选项 delta 累加,实时重算
- 价格计算 API 响应时间 < 300ms

---

## 4. 非功能需求

| 项目 | 要求 |
|------|------|
| 响应时间 | 页面加载 < 2 秒,API 响应 < 500ms,价格计算 API < 300ms |
| 文件大小 | 图片上传最大 10MB,文档上传最大 50MB |
| 缩略图 | 图片上传后自动生成 small(150×150)、medium(400×400) 缩略图;图册场景下额外输出 WebP |
| 数据安全 | 密码加密存储,Token 认证,分享链接密码哈希存储 |
| 数据库 | 仅支持 PostgreSQL 16(测试环境 Railway 提供,生产环境 Docker 容器) |
| 部署方式 | 测试环境:Vercel(前端) + Railway(后端+数据库),GitHub 自动部署;生产环境:Synology NAS Docker Compose 一键部署 |
| CI/CD | 推送到 GitHub main 分支后自动部署到测试环境(Vercel + Railway 原生支持) |
| 开机自启 | 生产环境 Docker 容器设置 restart: always |
| 数据备份 | 生产环境:支持数据库导出/恢复,媒体文件可通过 NAS Hyper Backup 备份 |
| 媒体文件存储 | 仅使用本地磁盘存储(测试环境 Railway 持久化卷,生产环境 NAS 本地磁盘) |
| 静态文件服务 | 测试环境:Vercel 托管前端静态文件,Railway 通过 WhiteNoise 服务 Django 静态文件;生产环境:Nginx 统一代理 |
| UI 设计风格 | 包豪斯(Bauhaus)风格定制主题:几何形态、功能主义、极简美学;微交互动效;微阴影系统;自定义字体(DM Sans + Inter + Noto Sans SC) |

---

## 5. 业务优化记录

> 本节记录基于客户反馈持续演进的优化项,详细方案参见 `aidlc-docs/optimization-plan.md`。

### 5.1 已实施的业务优化(早期阶段)

| # | 模块 | 优化内容 |
|---|------|---------|
| 1 | 产品管理 | 搜索扩展到描述、配置名称、配置属性等全字段 |
| 2 | 产品管理 | 支持 Excel 批量导入产品,提供标准模板下载 |
| 3 | 客户案例 | 行业分类改为办公空间 8 大细分行业 + 其他 |
| 4 | 内部文档 | 文件夹树增加删除按钮,支持删除空文件夹 |
| 5 | 内部文档 | 文档列表分页加载基础能力 |

### 5.2 本轮变更(2026-05)

**已采纳的新需求**

| # | 模块 | 优化内容 |
|---|------|---------|
| 1 | 产品图册 | 取消左侧分类树,改为多维搜索 + 悬浮筛选 + 即时查询 + MECE 区间 + 动态聚合(淘宝首页式) |
| 2 | 产品详情页 | 字段按《产品库字段描述.xlsx》重构(一级/二级级联 + 品类动态表单) |
| 3 | 产品详情页 | 电商式选购:配置选择器 + 实时指导价(双模式 Matrix / Rule) |
| 4 | 产品详情页 | 一键加入指定报价单(可选明细图,来自产品图集) |
| 5 | 产品详情页 | 上传产品配置 Excel,动态渲染配置选择器(含模板) |
| 6 | 报价单 | 明细 CRUD 完善;「新增明细」跳转产品图册(带 quoteId) |
| 7 | 客户案例 | 行业树筛选(8 大办公空间细分 + 其他),按名称排序 |
| 8 | 客户案例 | 详情图片继续平铺不分页,启用懒加载 / 缩略图 / WebP 优化 |
| 9 | 内部文档 | 设计资源 / 培训资料 / 资质文件支持在线预览;培训资料支持富文本上传与视频/音频 |
| 10 | 产品图册 | 多级类别展示(淘宝式标签),与悬浮筛选共用筛选状态 |
| 11 | 分享功能 | 增加批量分享与分享页品牌展示 |

**取消 / 剔除的需求**

| # | 模块 | 取消内容 |
|---|------|---------|
| 1 | 产品管理 | 分类体系改为 CATEGORY/BRAND 两棵强制分类树(取消;改为筛选项数据源) |
| 2 | 产品图册 | 双维度树形导航(取消;由"多维搜索 + 悬浮筛选"替代) |
| 3 | 内部文档 | 文件夹拖拽排序(剔除) |
